# SudarshanChakra — G-18 profile reference: security
# ==============================================================================
# Service selection is defined by Compose profiles on docker-compose.vps.yml.
# This file is documentation only (no merge required).
#
# Profile: security
#   Includes: postgres, rabbitmq, auth, alert, device, siren, api-gateway, dashboard, nginx
#   Excludes: mdm-service
#
# Example:
#   cd cloud && docker compose -f docker-compose.vps.yml --profile security up -d
#
# Or: ./cloud/deploy.sh --profile security
# ==============================================================================
services: {}
