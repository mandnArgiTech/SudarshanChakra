<?xml version="1.0" encoding="utf-8"?>
<!-- Debug only: allow HTTP to LAN edge Flask (e.g. :5000 snapshots). Release builds omit this overlay. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
