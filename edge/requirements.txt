# SudarshanChakra Edge AI Node — Python Dependencies
# ====================================================
# Installed inside ultralytics/ultralytics:latest Docker image
# which already includes: ultralytics, torch, torchvision, opencv, numpy

# Web GUI
flask==3.1.*
flask-cors==5.*

# MQTT client (publish alerts to VPS broker over VPN)
paho-mqtt==2.*

# USB-Serial for ESP32 LoRa receiver
pyserial==3.*

# Geometry library for point-in-polygon virtual fence checks
shapely==2.*

# Image processing (already in base image, pinned for safety)
Pillow>=10.0

# ONVIF PTZ camera control (discovery + pan/tilt/zoom)
onvif-zeep>=0.2

# Production WSGI server (Flask runs in a thread, not standalone)
gunicorn==23.*

# Optional: Multi-object tracking (for livestock containment)
# Uncomment if ByteTrack is needed for per-cow tracking
# bytetracker>=0.3

# Optional: Sliced inference for tiny objects (scorpion detection)
# Uncomment if SAHI is needed for specific cameras
# sahi>=0.11
