
!**/gradle/wrapper/gradle-wrapper.jar
!*.env.example
!.env.example
!gradle/wrapper/*.jar
!gradle/wrapper/gradle-wrapper.jar
!pa.env.example
#   cloud (Docker/PostgreSQL/RabbitMQ), AlertManagement (Python)
#   edge (Python/YOLO), android (Kotlin/Compose), firmware (ESP32/Arduino),
# ============================================================================
# Components: backend (Java/Spring Boot/Gradle), dashboard (React/Vite),
# SudarshanChakra — Monorepo .gitignore
# ── Android SDK (downloaded by setup script) ───────────────────────────────
# ── Android ─────────────────────────────────────────────────────────────────
# ── Audio files (AlertManagement PA — too large for git) ───────────────────
# ── Docker ─────────────────────────────────────────────────────────────────
# ── ESP32 / Arduino (firmware) ──────────────────────────────────────────────
# ── Environment & Secrets ──────────────────────────────────────────────────
# ── IDE / Editors ──────────────────────────────────────────────────────────
# ── Java / Gradle (backend + android) ───────────────────────────────────────
# ── Logs ───────────────────────────────────────────────────────────────────
# ── ML Models (too large for git) ──────────────────────────────────────────
# ── Misc generated / downloaded files ──────────────────────────────────────
# ── Node.js / React / Vite (dashboard) ─────────────────────────────────────
# ── OS ─────────────────────────────────────────────────────────────────────
# ── Python (edge, AlertManagement, cloud scripts) ──────────────────────────
# ── Snapshots ──────────────────────────────────────────────────────────────
# ── TLS Certificates & VPN ─────────────────────────────────────────────────
*$py.class
*.aab
*.ap_
*.apk
*.bin
*.class
*.cover
*.crt
*.csr
*.dex
*.ear
*.egg
*.egg-info/
*.elf
*.engine
*.env
*.flac
*.hex
*.hprof
*.iml
*.jar
*.jks
*.key
*.keystore
*.local
*.log
*.map
*.mp3
*.nar
*.ogg
*.onnx
*.p12
*.pb
*.pem
*.pt
*.py[cod]
*.pyo
*.sublime-project
*.sublime-workspace
*.swo
*.swp
*.tflite
*.war
*.wav
*~
.DS_Store
.cache/
.classpath
.coverage
.coverage.*
.cursor/
.cxx/
.env
.env.*
.eslintcache
.externalNativeBuild/
.flake8_cache/
.get-pip.py
.gradle/
.history/
.idea/
.mypy_cache/
.next/
.project
.pytest_cache/
.python-version
.ruff_cache/
.settings/
.tox/
.venv/
.vscode/
/tmp/farm-snapshots/
/tmp/snapshots/
Desktop.ini
Thumbs.db
[Dd]esktop.ini
__pycache__/
android-sdk/
build/
captures/
certs/
coverage.xml
dist/
docker-compose.override.yml
e2e/config/e2e_config.yml
edge/models/
ehthumbs.db
ehthumbs_vista.db
env/
hs_err_pid*
htmlcov/
local.properties
logs/
node_modules/
pip-delete-this-directory.txt
pip-log.txt
replay_pid*
runs/
venv/
vpn/*.ovpn
